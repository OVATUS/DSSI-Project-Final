Developed by: oversit
# Work Wai D (WWD) - Project Management System 🚀

**Work Wai D** คือเว็บแอปพลิเคชันสำหรับบริหารจัดการโปรเจกต์ในรูปแบบ Kanban Board (คล้าย Trello) พัฒนาขึ้นเพื่อช่วยให้ทีมสามารถติดตามสถานะงาน วางแผน และทำงานร่วมกันได้อย่างเป็นระบบและมีประสิทธิภาพ

## ✨ ฟีเจอร์หลัก (Key Features)

* **Project & Board Management**:
    * สร้าง แก้ไข และลบโปรเจกต์ (Board) ได้
    * ระบบติดดาว (Star) เพื่อปักหมุดบอร์ดที่สำคัญ
    * ดูรายชื่อบอร์ดทั้งหมดและบอร์ดที่ติดดาวได้ในหน้า Dashboard
* **Task & List Management**:
    * จัดการสถานะงานผ่าน List (เช่น To Do, Doing, Done)
    * สร้างการ์ดงาน (Task) พร้อมระบุรายละเอียด ความสำคัญ (Priority) และวันกำหนดส่ง (Due Date)
    * **Drag & Drop**: ลากย้ายการ์ดงานข้าม List หรือจัดเรียงลำดับได้อย่างลื่นไหล
* **Collaboration**:
    * เชิญสมาชิก (Member) เข้าร่วมบอร์ดเพื่อดูและทำงานร่วมกัน
    * มอบหมายงาน (Assign) ให้กับสมาชิกในทีม
    * ระบบคอมเมนต์ (Comments) เพื่อพูดคุยอัปเดตงานในการ์ด
* **Organization Tools**:
    * ระบบป้ายกำกับ (Labels) แยกประเภทงานด้วยสี
    * ระบบค้นหา (Search) ภายในบอร์ด
* **User System**:
    * สมัครสมาชิก (Register) และเข้าสู่ระบบ (Login)
    * จัดการโปรไฟล์และรูปภาพประจำตัว

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

* **Backend**: Python, Django 5.2
* **Database**: MySQL
* **Frontend**: HTML5, Django Templates
* **CSS Framework**: Tailwind CSS (ผ่าน `django-tailwind`)
* **JavaScript Framework**: Alpine.js (สำหรับจัดการ State และ Drag & Drop)

## ⚙️ การติดตั้งและเริ่มต้นใช้งาน (Installation)

### สิ่งที่ต้องมี (Prerequisites)
* [Python](https://www.python.org/) (3.x ขึ้นไป)
* [Node.js](https://nodejs.org/) (สำหรับการรัน Tailwind CSS)
* MySQL Database

### ขั้นตอนการติดตั้ง

1.  **Clone โปรเจกต์**
    ```bash
    git clone <repository_url>
    cd DSSI-Project-Final
    ```

2.  **สร้างและเปิดใช้งาน Virtual Environment**
    ```bash
    python -m venv venv
    # สำหรับ Windows
    venv\Scripts\activate
    # สำหรับ Mac/Linux
    source venv/bin/activate
    ```

3.  **ติดตั้ง Python Dependencies**
    ```bash
    pip install -r requirements.txt
    ```

4.  **ติดตั้ง Tailwind CSS Dependencies**
    ```bash
    python manage.py tailwind install
    ```

5.  **ตั้งค่าฐานข้อมูล (Database)**
    * เปิดไฟล์ `WWD/settings.py`
    * แก้ไขส่วน `DATABASES` ให้ตรงกับ MySQL ในเครื่องของคุณ (ชื่อ DB, User, Password, Port)
    * สร้าง Database ใน MySQL ให้เรียบร้อย

6.  **รัน Migrations**
    ```bash
    python manage.py migrate
    ```

7.  **สร้าง Superuser (สำหรับเข้าหน้า Admin)**
    ```bash
    python manage.py createsuperuser
    ```

## 🚀 การรันโปรเจกต์ (Running the Project)

เนื่องจากโปรเจกต์นี้ใช้ Tailwind CSS จึงต้องรัน 2 Terminal ควบคู่กัน:

**Terminal 1: สำหรับรัน Django Server**
```bash
python manage.py runserver
